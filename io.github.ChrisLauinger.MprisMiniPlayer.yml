app-id: io.github.ChrisLauinger.MprisMiniPlayer
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: mpris-miniplayer
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=session-bus
  - --device=dri
  - --filesystem=xdg-config/autostart:create
modules:
  - name: mpris-miniplayer
    buildsystem: meson
    run-tests: true
    config-opts:
      - --buildtype=release
    sources:
      - type: dir
        path: .
